sudo: required
services:
  - docker

before_install:
  - docker build -t ec-dev-image -f Dockerfile.dev .
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

script:
  - docker run ec-dev-image npm run test-ci
